---
- hosts: ec2-instance
  become: true
  tasks:
    - name: Install required system packages
      apt:
        pkg:
          - apt-transport-https
          - ca-certificates
          - curl
          - software-properties-common
          - python3-pip
          - virtualenv
          - python3-setuptools
        state: latest
        update_cache: true

    - name: Add Docker GPG apt Key
      apt_key:
        url: https://download.docker.com/linux/ubuntu/gpg
        state: present

    - name: Add Docker Repository
      apt_repository:
        repo: deb https://download.docker.com/linux/ubuntu focal stable
        state: present

    - name: Update apt and install docker-ce and docker-compose
      apt:
        pkg:
          - docker-ce
          - docker-compose
        state: latest
        update_cache: true

    - name: Install Docker Module for Python
      pip:
        name: docker
    - name: "Creating folder and copying html file"
      file:
        path: "/myWebApp"
        state: directory
      copy:
        src: "index.html"
        dest: "/myWebApp/"
    - name: Creating container using HTTPD Image
      docker_container:
        name: "MyWebServer"
        image: "httpd"
        #image: "654654452340.dkr.ecr.us-east-1.amazonaws.com/test:latest"
        state: started
        debug: no
        pull: yes
        exposed_ports:
        - "80"
        ports:
        - "8080:80"
        volumes:
        - /myWebApp:/usr/local/apache2/htdocs/

